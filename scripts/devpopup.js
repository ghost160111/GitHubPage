const openPop = (id) => {
    let popup = document.getElementById(id).style.display = "flex";
    return popup;
}

const closePop = (id) => {
    let popup = document.getElementById(id).style.display = "none";
    return popup;
}

